# otter-cli

Command-line interface for Otter.

This crate provides:
- Thin CLI wrapper around the daemon
- Interactive chat mode
- Daemon control commands

Currently stubbed. Full implementation pending.
